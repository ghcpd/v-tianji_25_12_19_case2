# Movie Ticket App

A small frontend-only React + TypeScript app for selecting movie seats and booking tickets. Uses Tailwind for styling and Zustand for simple state.

Commands:
- npm install
- npm run dev
- npm run test
- npm run coverage

This repository contains mock data and unit tests using Vitest and React Testing Library.
